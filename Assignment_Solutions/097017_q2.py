class Student:
    def__init__(self, name, student_id):
        self.name = name
        self.student_id = student_id
        self.assignments = {}

        def display_grades(self):
            if self.assignments:
                print(f"Grades for {self.name}:")
                for assignments, grade in self.assignments.items()
                print(f"-{assignments}:{grade}")
                else:
                    print(f"{self.name}has no grades yet.")

                    class Instructor:
                        def__init__(self, name, cource_name):
                            self.name = name
                            self.course_name = course_name
                            self.student = []

                            def add_students.append(students)
                            print(f"Student '{student.name}'added to the course'{self.course_name}':")

                            def assign_grades(self, student_id, assignment_name, grade):
                                student = next((s for s in self.students if s.student_id == student_id), none)
                                if student:
                                    student.add_assignment(assignment_name, grade)
                                    else:
                                        print(f"Student with ID '{student_id}'not found in the course")

                                        def display_students_grades(self):
                                            if self.students:
                                                print(f"Grades for all students in the course '{self.course_name}':")
                                                for student in self.student:
                                                    print(f"\nstudent: {student.name} (ID: {student.student_id})")
                                                    student.display_grades()

                                                    else:
                                                        print(f"No students enrolled in '{self.cource_name}'.")

                                                        def main():
                                                            Instructor = Instructor("Prof. Joseph", "introduction to Calculus")
                                                            while True:
                                                                print("\nCourse management System - Student Grades")
                                                                print("1. David Maina - Grade: B+")
                                                                print("2. Alice Johnson - Grade: A")
                                                                print("3. John Lee - Grade: C")
                                                                print("4. Display all Students and Their Grades")
                                                                print("5. Exit")

                                                                if choice =="1":



